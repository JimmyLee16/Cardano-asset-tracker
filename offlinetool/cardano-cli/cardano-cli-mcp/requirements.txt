# Cardano CLI MCP Server — Full Dependencies
# Install: pip install -r requirements.txt

# MCP SDK (includes transitive deps: pydantic, anyio, httpx, etc.)
mcp>=1.28.0

# --- Direct dependencies (pinned for reproducibility) ---

# Web framework (used by MCP's SSE transport)
uvicorn>=0.31.0
starlette>=0.40.0
sse-starlette>=3.4.0

# HTTP client (MCP transport)
httpx>=0.27.0
httpx-sse>=0.4.0

# Validation & settings
pydantic>=2.11.0
pydantic-settings>=2.5.0
annotated-types>=0.6.0

# JSON schema validation
jsonschema>=4.20.0
jsonschema-specifications>=2023.12.0
referencing>=0.28.0
rpds-py>=0.17.0
attrs>=22.2.0

# Async I/O
anyio>=4.0.0
sniffio>=1.3.0

# HTTP infrastructure
httpcore>=1.0.0
h11>=0.13.0
certifi>=2024.0.0
idna>=3.0

# Environment / config
python-dotenv>=0.21.0

# JWT auth (MCP dependency)
PyJWT[crypto]>=2.10.0
cryptography>=3.4.0
cffi>=1.14.0
pycparser>=2.16

# Multipart form support
python-multipart>=0.0.30

# CLI framework (uvicorn dep)
click>=7.0

# Windows COM support (uvicorn/pywin32)
pywin32>=306;platform_system=="Windows"

# Typing backport (Python < 3.12)
typing_extensions>=4.0.0
typing-inspection>=0.4.0
